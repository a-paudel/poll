version: '3.3'

services:
  vote:
    restart: always
    build: .
    env_file:
      - .env
    expose:
      - $PORT
    volumes:
      - ./data:/app/data
    labels:
      - traefik.enable=true
      - traefik.http.routers.vote.rule=Host("poll.paudel.me")
      - traefik.http.routers.vote.entrypoints=websecure,web
      - traefik.http.routers.vote.tls.certresolver=certbot
    networks:
      - backend
networks:
  backend:
    external: true
